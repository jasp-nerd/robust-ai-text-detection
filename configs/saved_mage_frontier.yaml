# Eval-only: saved mage checkpoint on the frontier (Qwen3) test set.
name: saved-mage-frontier
seed: 0
model:
  type: encoder
  model_name: results/runs/encoder-modernbert-base-mage/model
  max_tokens: 512
  batch_size: 32
train: null
eval:
  - dataset: frontier
    split: test
