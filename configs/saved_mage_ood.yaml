# Eval-only: the saved MAGE-only ModernBERT checkpoint on the fully-OOD sets.
name: saved-mage-ood
seed: 0
model:
  type: encoder
  model_name: results/runs/encoder-modernbert-base-mage/model
  max_tokens: 512
  batch_size: 32
train: null
eval:
  - dataset: m4gt
    split: test
    sample: 15000
  - dataset: frontier
    split: test
